{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.rs) PATH=\"/opt/homebrew/opt/rustup/bin:$HOME/.cargo/bin:$PATH\" rustfmt --edition 2021 \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting Rust file"
          }
        ]
      }
    ]
  }
}
