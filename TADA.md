# TADA

1. [x] The catcher should be catching pitched balls that aren't hit. — `flow::catcher_receives` stops clean takes/whiffs in the mitt (glove-up + pop); balls in the dirt (dropped third) still get away.
2. [x] The current play, including a home run, should have ended before the next batter is up to bat. — the result pause now waits for every runner rig to finish its path (`runner::RunnersSettled`), trot included, with a 20 s safety cap.
3. [x] At the end of a play, steal or attempted steal, if there is still a runner on any base. Wait 5 seconds to give the opportunity to make an attempt to steal. — the pre-pitch steal window (`Ruleset::steal_window_secs`, 5 s) gates the pitch whenever a runner is actually in a position to steal; a pickoff out takes the normal result pause, after which runners still aboard earn a fresh window.
4. [x] Runners on base should understand the concept of leading off. There should be controls to steal. Likewise, there should be controls for picking off players on base. — runners take a real leadoff (walk off the bag); offense holds Down to stretch it (guaranteed steal jump on the pitch), defense presses action during the window to throw over (`rules::attempt_pickoff`: out only if the lead was stretched). The CPU plays both sides.
5. [x] When the ball is hit into the air, there should be a contextually sized ring directly underneath for better indication where and when it will land. — `fx.rs` landing ring at the live-predicted touchdown, radius shrinking with remaining hang time.
6. [x] There should be umpires. — data-driven `FieldSpec::umpire_positions` crew in fixed blacks (4 in the stadium, 1 on the lawn); the plate umpire crouches through the duel.
7. [x] There should be controls for throwing a variety of pitches. — five-pitch arsenal by held-aim octant: up fastball, down curveball, left slider, right sinker, neutral changeup (duel-panel legend updated).
8. [x] There should be a roster of players on each team, with alternates for substitutions. There should be methods to stop the game to make them at any time when the ball is not in play. — `roster.rs` (9 starters + 4 bench per team); Esc/P (gamepad Start) between plays opens the substitution board (`subs.rs`).
9. [x] Team members should have names and numbers assigned to them, with the last name and number appearing on the back of their jersey (and number on front and shoulders) like typical baseball jerseys. — `jersey.rs` draws each card with a built-in 5×7 bitmap font into runtime textures: name + number on the back, number on chest and both shoulders.
10. [x] We should be prepared to use different player models, including humanoid-like ones, that have realistic running, swinging, catching, diving, sliding, throwing etc. animations. — `Theme::player_model` (`PlayerModelId`) seam in the rig builder, plus new `Dive`/`Slide` clips and a `root_pitch` body-lean channel; a humanoid model plugs in as a new arm with richer poses behind the same `AnimClip` names.

## Batch 2

11. [x] Maintain a document of research you've gathered on baseball; reference it and run more research online when needed. — `docs/BASEBALL.md`: web-sourced field/mound/base/groundskeeping specs with sources, mapped to the code that models them; CLAUDE.md points to it as the convention for future features.
12. [x] The batter should be facing the plate. — the batter rig now stands side-on to the pitcher in the box, facing home plate (−X), per the batter's-box notes in docs/BASEBALL.md.
13. [x] The catcher should catch the ball if it's a ball or a (non-foul ball) strike. — already in place from `flow::catcher_receives` (takes and swinging strikes end in the mitt; fouls come off the bat and stay live; the dropped third stays loose in the dirt); verified in the browser.
14. [x] The player-at-bat/pitcher-ready view needs to be from the catcher's point of view. — the duel camera now sits just over the crouched catcher's helmet (eye ~2.3 m, right behind the plate), strike zone floating above his head, delivery coming straight in; verified with browser screenshots on the wasm build.
15. [x] We need to see the batter swing the bat. — new `AnimClip::BatterSwing` drives the batter's arms through the swing alongside the bat-pivot sweep, and the run-out rig now waits hidden through a short `RunDelay` (0.4 s; 0.9 s on homers) so the real batter visibly finishes the follow-through before the swap — fouls no longer make him vanish at all.
16. [x] Don't follow the ball until 1 second after the batter has made contact with it. — `camera::BALL_FOLLOW_DELAY` holds the plate framing for the first second after contact before cutting to the ball.
17. [x] There should be an umpire behind the catcher. — already in place (`FieldSpec::umpire_positions`, plate umpire crouching behind the catcher); kept behind the new catcher-POV lens.
18. [x] There should be a pause button ("P" / gamepad Start) that brings up a pause menu. — already in place via `subs.rs`; the board is now titled PAUSED with the substitutions section beneath, verified rendering on the wasm build.
19. [x] There should be a pitcher's mound. — upgraded to regulation per docs/BASEBALL.md: 18 ft diameter, 10 in high, a low skirt approximating the 1-in-per-foot slope, and the white 24×6 in pitching rubber.
20. [x] The bases need to be more defined. — regulation 18 in bags (2023 rule), raised with a touch of glow, plus the ~13 ft dirt cutout circles at each bag and around home plate.
21. [x] The outfield and infield should have more realistic textures. — procedural runtime textures (no asset files): mow-striped grass on the outfield and lawn, speckled clay on the dirt diamond/basepaths/mound, with a grass infield inside the dirt basepath band.
22. [x] Player models should be humanoid-like with realistic animations, not blocky rigs. — a skinned glTF player model (`src/game/models/player.glb`) built by `tools/build_player.py` → `assets-src/player.blend` → `tools/export_glb.py`, embedded via `embedded_asset!` and pinned by `tests/model_contract.rs` against `model_assets::CLIP_TABLE`; `animation.rs` now drives Bevy's `AnimationPlayer`/`AnimationGraph` with 150 ms cross-fades behind the same `AnimClip`/`Playing`/`MoveIntent` API (the old procedural `Blocky` rig stays as a fallback arm); `--features dev` file-watches the `.glb` for Blender hot-reload.
23. [x] Batters start rounding the bases as soon as they hit the ball; runners on base run intelligently by outs and fly-out risk. — the run-out rig activates at contact, and a pure `rules::runner_break` (sourced in docs/BASEBALL.md §Baserunning: two-out go, forced-grounder go, halfway on catchable flies, tag-up on deep) drives `runner.rs` break choreography, with the call/outcome machinery byte-untouched.
24. [x] The controls at the bottom of the screen only show in a dialogue box when paused. — the help text moved into a `ControlsDialog` card under the pause board, painted at spawn per the wasm UI rule.
25. [x] HUD boxes to the four corners (score BR, player BL, pitching TR, on-base TL). — corner anchors at the existing 14 px margins in `ui.rs`.
26. [x] White chalk: batter's-box outlines and foul lines out through first/third. — regulation 4×6 ft boxes and 3 in foul lines painted as flat decals from `foul_line_span`, layered above every ground decal (`field.rs` chalk consts + ordering test).
27. [x] Several at-bat camera views toggled by a button, with the catcher/ump invisible when in the way. — **V** cycles CatcherPov / BehindPitcher / BattingZoom / BroadcastPlate (`DuelView`), with a pure occlusion cone hiding `CatcherRole`/`PlateUmpire` only when they block the active broadcast view.
28. [x] The batter holds the bat correctly, including swinging it to hit the ball. — the `Bat` bone now sits in the hands (rest-gap fixed), a looping `BattingStance` clip poses a coiled two-handed stance through the duel, and `BatterSwing` re-keyed so both hands ride the bat through a level sweep (left arm solved per-frame against the grip).
29. [x] The `src/game` tree was a flat 31-file module with files running past 3,000 lines — reorganize it for maintainability without touching gameplay. — migrated to Rust edition 2024, split into a layered `core`/`sim`/`present`/`meta` hierarchy behind the existing `game::<module>` facade (largest file dropped from 3,177 to 770 lines — `core/rules/resolve.rs`, a named, accepted exception to the ~700-line target), added a repo-wide clippy lint table, hoisted shared geometry/physics constants into `core`, and applied a `#[must_use]` pass on pure `rules::` result types — zero behavior change, `tests/balance_sim.rs`'s offensive economy untouched. See `docs/superpowers/specs/2026-08-19-layered-refactor-design.md`.

## Batch 3 — playtest & production-readiness fixes (2026-08-20)

30. [x] Playtest TODO 1 — the live ball was nearly invisible in outfield chase framing. — `BallHalo` in `src/game/present/fx/particles.rs`: an unlit theme-trail-tinted shell parked on the ball during `Phase::InPlay`, scaled by broadcast-camera distance (≈ screen-constant, clamped ball-size…0.9 m) so the chase reads at range; hidden otherwise.
31. [x] Playtest TODO 2 — hidden HUD duel cards ghosted as dark boxes against the sky on wasm. — root cause: the tint-and-blank idiom left stale glyphs/chrome rendering dimly on wasm/WebGL2 (pixel-sampled at ~10–15 % opacity); `update_duel_panels` now flips root `Visibility` (the pause-board mechanism), belt-and-braces keeping every alpha nonzero.
32. [x] Playtest TODO 3 — "zone box reads as a ground-to-chest column": verified correct, no change. The drawn zone is `ZONE_LOW..ZONE_HIGH` (0.45–1.28 m), plate-wide/deep, pinned by `zone_wireframe_matches_rulebook_dimensions`; the column read was the near-face fill panel + perspective.
33. [x] Playtest TODO 4 — the plate umpire clipped the parked catcher-POV lens during result pauses. — `hide_occluders` now gates on the same `duel_framing_wanted` predicate the broadcast rig holds framing with, and catcher-POV hides both plate rigs outright (the umpire stands up *behind* the eye, where the look-ahead cone can never flag him). No clipping observed across a full browser session post-fix.
34. [x] Audit TODO 12 — wasm panic hook. — `src/main.rs` installs a `panic::set_hook` on wasm that `console.error`s the message and posts `bb-panic:<msg>` to the page; `web/index.html` swaps the dead canvas for a "Something broke — Reload" card with the panic text. Verified via synthetic message (screenshot 13).
35. [x] Audit TODO 13 — WebGL context-loss handling. — `web/index.html` attaches `webglcontextlost` to the Bevy canvas post-init and shows the reload card. Verified via `WEBGL_lose_context` (screenshot 14).
36. [x] Audit TODO 14 — mobile/touch stance. — coarse-pointer-only devices get a dismissible "needs keyboard or gamepad" overlay instead of a silently unresponsive canvas. Verified via device emulation (screenshot 15).
37. [x] Audit TODO 15 — loading progress + wasm-opt. — `web/index.html` streams the wasm fetch with a real progress bar (percent against Content-Length when served raw; MB counter under gzip where decoded-byte percent would lie) and a "Compiling…" stage; `pages.yml` gained a `wasm-opt -Oz` step (bulk-memory + nontrapping-fp flags). Verified under Fast-4G throttle (screenshot 12). Residual: raw size still ~43 MB pre-opt vs the ≤30 MB budget — follow-up filed.
38. [x] Audit TODO 19 — `src/game/.DS_Store` untracked; `.DS_Store` gitignored.
39. [x] Audit TODO 28 — favicon (⚾ emoji SVG data URI) in `web/index.html`.

## Batch 4 — release-polish backlog (2026-08-21)

40. [x] TODO 16-18, 25 — release hygiene. — `LICENSE-MIT` + `LICENSE-APACHE` matching
    `Cargo.toml`'s declared dual license; `README.md` rewritten against the four-layer map,
    the five-pitch/steal-duel controls, the live Pages link, and current build commands;
    `pages.yml` gained a post-deploy smoke-test job (curls the live page and wasm artifact,
    asserting 200 / `application/wasm` / byte size matching the built binary); the menu
    shows `v0.1.0` (`env!("CARGO_PKG_VERSION")`) bottom-right, verified in the browser.
41. [x] TODO 20 — browser focus-loss auto-pause. — tab-hidden (`WindowOccluded`) or focus
    loss arms a pending pause that lands at the first dead-ball moment (reusing the subs
    pause path) instead of freezing `Time<Virtual>` under a live ball, so the juice
    invariant stays intact; regaining focus first disarms it.
42. [x] TODO 22 — subs-board gamepad bindings. — D-pad mirrors the arrows, South swaps,
    North switches team, Start already resumed via `pause_pressed`; hint line updated.
    Hardware pass still owed (TODO 33).
43. [x] TODO 23 — reduce-motion accessibility toggle. — new serde-defaulted
    `Settings::reduce_motion` + REDUCE MOTION settings row; `juice::motion_enabled` gates
    hit-stop/slow-mo and the camera-kick impulses, reading (never writing) the harness's
    `JuiceDisabled`. Verified end-to-end on web incl. localStorage persistence.
44. [x] TODO 24 — settings schema decision. — no version field until a genuinely breaking
    rename (every change so far is additive + serde-defaulted); an unparseable store is now
    preserved under `settings.json.bak` / the `.bak` localStorage key before defaults load,
    with a regression test.
45. [x] TODO 26 — pause-refusal feedback. — a refused mid-play Esc flashes PLAY IN PROGRESS
    through the existing banner channel (`PlayBanner::new` made pub for meta emitters).
46. [x] TODO 21 — explicit Msaa/shadow choices. — camera spawns `Msaa::Sample4` native /
    `Sample2` wasm; `DirectionalLightShadowMap` 2048 native / 1024 wasm (was implicit
    defaults everywhere). Measured wasm: 120 fps display-capped at 2560×1488 on M4 Max.
    Native F1 readout owed (TODO 33).
47. [x] TODO 30 — fielder set-spot drift (the CRUZ-at-home bug). — root cause: on an
    instantly-resolved play (liner caught the same frame covers went out),
    `fielding::return_to_spots` fired while every fielder was still within its already-set
    tolerance, sent nobody home, consumed its one-shot state, and left the cover
    `MoveIntent`s live — the defense then ran to the bases *after* the play and parked.
    Fix: play end voids every outstanding fielding order. New `e2e_fielder_spots`
    regression (full CPU game, no fielder off-spot 3+ consecutive deliveries).
49. [x] TODO 32 + deploy boot regression — wasm-opt sizing and the binaryen fix. — CI
    wasm-opt (first run) cut the deployed binary 43.3 MB → 16.9 MB raw (−61%, inside the
    ≤30 MB budget); gzip wire 5.6 MB (was 7.96 MB, inside ≤10 MB). But apt's binaryen 108
    mangled wasm-bindgen 0.2.126's externref table — the deploy died at init with
    `WebAssembly.Table.grow(): failed to grow table by 4` (the TODO-12 panic surface caught
    it with a clean reload card). `pages.yml` now pins the upstream binaryen `version_132`
    release with `--enable-reference-types`; the exact flag set verified booting locally.
50. [x] TODO 6 — settings card translucency. — the open card now paints `panel_bg` at full
    alpha (theme panels carry ~0.85 alpha for field layering); menu text no longer bleeds
    through. Verified on web.
51. [x] TODO 7 — the "floating tan ring" at the batter's chest. — identified: STONE #21's
    gold-chain gear prop (`meta/gear.rs`), whose 7 cm spine-bone standoff detached visually
    in the stance lean. Re-anchored to (0, 0.24, 0.135); reads as worn gear on both themes.
52. [x] TODO 5 — bat parked vertically behind the back between pitches. — the `Idle` clip
    never keyed the `Bat` bone, so idle frames showed the rest pose (solved for
    raised-arm stances). `Idle` now keys the barrel down-forward — a loose at-the-side
    carry (rebuilt `player.blend`/`player.glb`, `model_contract` green). The
    shoulder-quad "float" sighting resolved with the chain fix (51).
53. [x] TODO 8 — chest number wrapping onto the torso side. — the chest quad standoff
    dropped 0.16 → 0.14 off the spine bone, killing the parallax that read as
    side-face lettering at oblique angles.
54. [x] TODO 9 — catcher-POV batter dominance. — `duel_eye` steps 0.4 m (0.35 m FrontYard)
    toward first base plus a half-step closer to hold the 80–90% batter-height contract;
    the zone box and pitcher now fully clear the batter's silhouette (screenshot
    03-duel-offset-eye). Camera framing tests re-pinned.
55. [x] TODO 27 — colour-blind emulation pass. — deuteranopia + protanopia (SVG
    colour-matrix over the canvas) on both themes: team split stays legible (yellow-olive
    vs lavender), jersey lettering backstops, B/S/O dots are position-labelled. Recorded in
    `theme.rs` module docs; screenshots in `docs/agent/playtest/2026-08-21/`.
56. [x] TODO 31 — BallHalo oversized-disc sighting. — not reproduced across a browser
    session; clamp math re-verified by inspection (dist × 0.007 into [0.05, 0.9] m world
    radius; the multi-camera fallback degrades to the *minimum*, not max). Closed as
    unreproducible; reopen with a screenshot if it recurs.

## Batch 5 — release robustness (2026-08-21)

57. [x] TODO 34 — headless boot check in the deploy pipeline. — the smoke test could not
    catch a wasm that dies at init (the binaryen-108 clamp passed every byte-level signal —
    TADA 49). `tools/web_boot_check.mjs` now boots the live page in headless Chrome
    (SwiftShader WebGL2, raw CDP over Node 22's built-in WebSocket, zero deps) as a final
    `pages.yml` smoke-test step: PASS needs the canvas up and the loading overlay gone,
    held 10 s for late panics; FAIL on the "Something broke" card or a stalled boot.
    Proven both ways before shipping: the healthy artifact boots, and a wasm with the
    externref table max clamped to min (the exact 108 corruption, byte-level simulated)
    fails with `WebAssembly.Table.grow(): failed to grow table by 4`. First CI run booted
    the live deploy in ~34 s on the runner; verified independently from this machine.

## Batch 6 — coach follow-ups (2026-08-24)

59. [x] The Coach observer recognized dropped-third plays by banner text ("DROPPED 3RD") — the one string-match in the snapshot builder. — `Play::last_strike_call` is the new read-only seam (set at both `add_strike` sites, cleared at the PrePitch reset); the observer's untouched-pitch exemption now consumes the umpire's decision directly, and the banner `EventReader` is gone from the sampler.
60. [x] wasm autoplay always played 9-inning CPU-vs-CPU attract games (innings/script were env-configurable natively only). — the native env switches now ride the page URL on wasm: `?innings=1&script=balanced` (web-sys `Location`), one shared `AutoplayConfig` for both targets. Verified live in the browser: a `?innings=1&script=balanced` game ended at the inning-1 walk-off (`bb-state game-over` + final `COACH_REPORT`, 4,342 samples, zero findings) and the attract loop restarted with the same parameters.
58. [x] One full-suite run failed `e2e_fielder_spots` with a "parked fielder" 3-delivery streak that would not reproduce. — Closed with three pieces. (1) The schedule's ambiguous orderings were the reproducibility hole: `tests/ambiguity_audit.rs` found 130 gameplay-ambiguous pairs (the unchained ball tuple, flow<->fielding<->runner cross-chain order, locomote vs the sim, gamepad-hotplug vs Controllers readers); `GameplayOrder` (Input -> Flow -> Ball -> Fielding -> Runners -> Rigs, chained in GamePlugin) pins the canonical same-frame event flow and the audit test now gates that no gameplay pair is ever ambiguous again. (2) The pinned build reproduced the failure deterministically, and test-side instrumentation showed every off-spot fielder across 222 deliveries carried the correct return order — the "parked" fielder was legally jogging home from a deep backup station across three quick deliveries (2.7 m out on the third); the probe now counts a fielder only when off-spot AND not ordered home, which catches the original TODO-30 stale-order bug *more* precisely. (3) Cross-process trajectory variation is Rapier's documented ASLR-seeded sub-ULP perturbation (see bands.md) — bounded by the balance bands, not fixable without `enhanced-determinism`. Probe re-run 3x across processes: green.
10. [x] Passive human batters drew CPU walk chains (observed 7 BB/inning in the browser; 10 BB in the headless passive-half probe). — Fixed by the paired behind-in-the-count package in `sim/ai.rs`, exactly as the 2026-08-21 revert prescribed: at 2+ balls the pitcher pulls in (scatter ×0.4, get-it-over arsenal — and the decisive detail: the get-it-over heater aims mid-zone (+0.3), because the standard +0.55 fastball bias plus backspin lift parks at the top edge, which was the residual miss) while the batter compensates in the same counts (timing draw ×0.65 — grooves get barreled, not whiffed — and chase ×0.5). New `tests/e2e_passive_walks.rs` probe: 10 walks → 0 across three cross-process runs (≤3 asserted). `balance_sim` bands held across repeat N=40 runs; helpers unit-tested (the two arms engage together at the same threshold by construction).

## Touch controls (2026-08-28)

61. [x] Support play on mobile devices with precision touch swing mechanics — several selectable solutions, chosen before starting the game. — Four schemes behind a persisted `Settings::touch_scheme` (menu line **G Touch swing** / settings row **P1 TOUCH SWING**): **Tap** (tap = swing at that instant, tap position aims; Classic grading), **Flick** (flick up through the ball, the flick moment is the swing, its direction the aim; Classic grading), **Hold + release** (hold loads the meter, drag aims, lift swings; Swing Meter grading), **Zone Pad** (left pad maps the finger absolutely onto the strike zone via the new `TeamIntent::cursor` channel, right SWING button fires; PCI grading). One touch translator (`meta/touch.rs`) turns raw `Touches` into a `TouchIntent` merged into P1's `Intents` slot, so the Director/scripts/matrix cover touch automatically; on defense every scheme shares drag-to-aim + second-finger-action. Menus, the settings screen, and game-over are all tappable (`Button`/`Interaction` — also giving mouse support for free); an in-game overlay draws the pad/button from the same layout fns the hit tests use, plus a per-scheme hint line. Web shell: `touch-action: none`, no-zoom viewport, the touch-only blocker replaced with a controls primer. Verified: full `cargo test` green (266 unit + all e2e suites), wasm check green, and a live browser run — tapped the menu to cycle schemes and start a 1P game, then pitched by real CDP touch events (drag-up aim + second-finger tap → strike-zone miss called a ball on the count HUD). Not visually exercised in-browser: the Zone Pad overlay while batting (bottom half never reached); its geometry and mapping are unit-tested.

62. [x] Review-fix-repeat hardening of the touch controls (four /code-review cycles, ~40 verified findings fixed). — Correctness: swing gestures and runner steering isolated via pitch-window re-anchoring (a lingering post-flick finger no longer forces runner calls; a windup send-drag no longer poisons Hold+Release aim); flick re-arms every pitch and measures per-frame displacement (120 Hz touch streams halved the speed via `Touch::delta`); Tap's aim no longer clobbered by a resting finger; Zone Pad is portrait-safe (min-dimension sizing — the button sat 96% inside the pad at 390×844), always absolute (center-pinned sticky cursor kills stick-drift into the PCI cursor), hands a half-inning-flip finger to the pad, and fires an anticipatory SWING press at delivery; orphaned fingers are adopted so touch never goes dead mid-hold; touch survives a vanished gamepad; a press held through the settings overlay can't start a game on close. Reachability: `Off` now means *basic taps* (drag = aim, tap = action — a touch-only phone can no longer soft-lock into an unplayable game), and a touch pause button (routed through `subs::PauseTapped`, sharing the dead-ball gate) pauses/resumes. Architecture: one `TeamIntent::overlay` merge policy, `TouchIntent` is a `TeamIntent` newtype, `touch::touch_team` is the single ownership predicate (autoplay builds simply have no touch owner — no settings mutation, persistence stays on), `MenuDirty` single-rebuild-point with deterministic menu↔settings ordering, change-honest settings edits via `set_if_neq`, wasm extraction kept alive by per-frame root repaints (per the 2026-08-27 bisect). Coverage: new `zone-pad` script + `one_player_zone_pad_touch_scheme` matrix cell exercise the scheme override and the absolute-cursor channel on every `cargo test`; `ScriptAction::Cursor` makes the channel Director-scriptable. Deliberate declines (recorded here so they aren't re-litigated): game-over stays any-input + 1 s grace; menu rebuild-per-press stays (event-frequency); the full `paint_settings_screen` change-gate stops at the text section (root/card repaints are wasm-load-bearing); `InputSource::Touch` remodeling remains future work.

63. [x] Review cycles 5–7: runtime ownership, chrome correctness, and translator coverage. — Architecture: touch ownership became a runtime-resolved `input::TouchOwner` (Controllers + Director presence — a directed pseudo-human slot is never touch-owned), deleting the `cfg!(autoplay)` gate so `cargo test --features autoplay` behaves identically to plain `cargo test`, restoring settings persistence in autoplay builds, and keeping `style_for` a pure function fed by parameter; the Zone Pad's anticipatory press moved into the translator so it reaches the adapter as an ordinary `Intents` edge (the sim layer keeps zero device knowledge); `TouchIntent` now lives with the seam that consumes it; the wasm keep-alive rule became one shared `KeepAliveUi` marker + wasm-gated system instead of three hand-copied per-frame repaints; `set_text_if_neq` is the one Text guard; a single `chrome_claims` predicate covers every claim site. Correctness: pause taps route through `subs::PauseTapped` behind the same dead-ball gate as Esc/P/Start, with first-holder semantics on the pad and SWING button, a pre-frame-`seen` liveness bit so the revealing touch can't press the invisible button, a Paused-state raw-region path (bevy_ui attributes multi-touch presses to the first held finger), carve-outs from all gameplay claims, and clears on exiting the setter states; Zone Pad chrome hides once the ball is live (regions release to runner steering); a windup tap's aim survives the flip frame; keyboard keeps filling a vanished pad's slot (TODO-72 rule, now also during the hotplug window); the center-pin emits only when a touchscreen is real, so keyboard PCI steering is never locked out. Coverage: three translator integration tests drive real `TouchInput` events through Bevy's input plugin into the gesture state machine (tap swing + seen bit, flick re-arm across pitches, pad claim/sticky/button edge); the matrix's zone-pad cell pins that a Director-driven slot has no touch owner. Declines recorded: bevy_ui's first-finger multi-touch attribution on menu Buttons (engine-level; single-finger menu taps are the norm), per-slot `InputSource::Touch` remodel (standing), `read_touch` per-scheme dispatch restructure (stable after hardening; revisit only with a fifth scheme).

64. [x] Review cycle 8: the device model owns touch ownership; chrome and paint edge cases. — Architecture: the separate `TouchOwner` resource folded into `Controllers::touch_team` (ownership lives on the device model it derives from; `resolve_touch_owner` writes it guarded each frame), and the overlay's six marker components collapsed into two kind enums (`TouchChrome`/`TouchLabel` — one paint query, no `Without` walls). Correctness: `paint_touch_overlay`'s output-gating `Local`s survive the between-games respawn (`Added<TouchOverlay>` forces the fresh tree's first paint — a new game whose first key matched the old game's last one never painted); the pause-chrome finger is remembered by id so a lingering chrome finger is never adopted as the stick or counted as an action hold; a same-frame windup tap's aim is guarded against the resting finger's drag; the settings cursor parks on every close path. Verified: 271 unit tests, the 8-cell matrix, balance sim, wasm check, and `--features autoplay` all green. Two angles (conventions, cross-file tracer) returned empty.

65. [x] Review cycle 9: four real touch-translator bugs, a live-browser catch, and the MenuDirty deletion. — Correctness (line-by-line finder): a defense action tap no longer eats the drag aim on its edge frame (the guard keyed on `next.action`, which the second-finger arm also sets — every aimed touch pitch/throw became unaimed at the exact frame flow samples aim; now a `tap_aimed` bit tracks only real tap aims); a windup swing-tap no longer clobbers a deliberately deflected send-drag (flow re-reads the send every frame); one flick swing per *pitch*, whichever finger (`TouchGestures::flick_spent` pitch-level latch replaces the per-finger flag a lift-and-swap or orphan adoption could reset mid-flight); a SWING press held through a pitch's Result re-acquires the button at the next PrePitch (the handoff only checked the pad — the visibly-held button was dead for the whole next pitch). Live-browser catch: an instantaneous tap (down+up between two frames — real fast taps and synthetic browser taps both do it) fired the bare-tap action but never flipped the `seen` device bit, leaving all touch chrome hidden; `read_touch` now checks `just_pressed` too. Refactors: `MenuDirty` deleted — the menu rebuild gates on the displayed resources' own change ticks, which also deletes the `refresh_menu_on_settings_close` edge-watcher and its two scar-tissue workarounds; `GameOverGrace` is a timestamp, not a ticked Timer; cursor parking has one home (top of `toggle_settings`, before the toggle); the pause-region claim is one shared helper for Playing and Paused (the Paused path now also records the chrome finger); `pad_zone_cursor` routes through `screen_to_aim`; `update_controller_status` uses `set_text_if_neq`; `KeepAliveUi` docs state the adoption criteria; test `Controllers` literals use struct-update. Verified live on wasm (removed-behavior finder's ask): after long idles the settings screen paints completely (all rows, border, values — the two-background keep-alive carries the whole subtree) and the touch overlay reveals on first tap (hint line + pause button, screenshotted); 5 new regression tests (276 unit total). Declines re-affirmed: settings-row enum remodel (recorded in 62), per-write paint guards stay (house change-honesty style), `style_for`'s touch override stays until a second style-dictating mechanism exists, tap-framework unification waits for a third overlay. Still not visually exercised: the Zone Pad pad/button while batting (same paint path as the verified pause chrome; geometry unit-tested).

66. [x] `cargo test --features autoplay` green end-to-end for the first time — the self-driver moved out of the library. — Cycle 9's full-suite verification widened to `--no-fail-fast` and caught `balance_sim` failing under the autoplay feature (K% 30.9, zero walks — a completely different offensive economy; earlier "autoplay green" claims had only ever reached the unit tests because cargo stops at the first failed target). Bisected by stash to pre-feature main (`d8984ba`): identical failure — pre-existing since the feature's birth, not a touch-loop regression. Root cause: `GamePlugin` registered `AutoplayPlugin` under the feature, so every test harness built from the lib inherited the attract-mode menu driver (`navigate_menus` holding **1**/**Enter**) and the Startup `Director` insert, which hijacked each harness's own game setup. Fix: the plugin now registers in `src/main.rs` — only the real app self-drives; native and wasm autoplay runs are untouched (both go through the bin), and the rule is a CLAUDE.md invariant. Bonus flake fix while verifying: the two-thumb flick test pressed both fingers in the same frame, and `Touches` hands same-frame touchdowns to the claim loop in random HashMap order — the "wrong" thumb sometimes became the stick; fingers now press on separate frames, and all flick tests sweep 450 px with explicit down-resets instead of leaning on tiny real dt (10/10 repeat runs green). Verified: full plain suite green (bzye60m3e record), full autoplay suite green (31/31 targets, no-fail-fast), wasm check green.

67. [x] Review cycle 10: five more real touch bugs (three found only by tracing), ownership needs a touchscreen, and the menu gets one build path. — Correctness: the Zone Pad's absolute cursor no longer keys on the session-permanent `seen` bit (one incidental brush on a touchscreen laptop locked keyboard/stick PCI steering out for the rest of the session — flagged independently by three finders; now it emits only while a pad aim is live this at-bat or fingers are actually on the glass, with a regression test); touch ownership itself now requires a seen touchscreen (`resolve_touch_owner` — selecting a scheme on the menu no longer hijacks a desktop keyboard player's configured batting style with zero touches ever), runs in MainMenu too, and re-resolves on the `game_start()` transition (frame one no longer reads the stale `None` a fresh `assign_controllers` wrote); `detect_touchscreen` sets the device bit from menu taps (outside `Playing` only — inside, `read_touch`'s pre-frame reveal discipline owns it), so a phone player's first gameplay touch isn't spent on detection; the "effective P1 style" now has ONE encoding (`batting::touch_style_override`), consumed by `style_for`, the settings row's "[set by touch swing]" paint, and its edit guard — the row can no longer claim an override the adapter isn't applying; `pause_finger` survives a pause (a drifted still-held chrome finger was adopted as the stick on resume) and its lifted-release check moved into the shared claim so Paused clears recycled ids too; `pause_live` is re-decided before every `read_touch` early return (an ownerless game froze it latched true — invisible-but-live button) and cleared at `game_start()` (the one-frame stale window on a new game's first frame); the Zone Pad handoff scans every resting finger, not just the tracked stick (an untracked second thumb on the pad was dead for the whole at-bat); a Tap-scheme touchdown claims its finger spent (one physical windup tap fired two action edges — touchdown and quick release); the paused game hides the pad/SWING/hint chrome (drawn-but-dead controls over the pause board); `menu_tap` stops after `OpenSettings` (a second same-frame press could cycle an option or start a game under the fresh overlay). Structure: `spawn_menu` deleted — `rebuild_menu_on_change` builds when the tree is missing and skips the launch frame (`NextState` pending), one build path with no double-build per entry; `cycle_row` ends in `unreachable!` like its paint sibling (a rendered row can't silently ignore taps); `Phase::pre_contact()` names the duel-window predicate (8 inline copies converted; the camera's exhaustive match keeps its arms); `rules::aim_to_world_x` is the one aim→world-X encoding (pitch target, hit spray, PCI steering, throw direction, pad cursor); `ui::set_color_if_neq` joins `set_text_if_neq`; `despawn_all` converted its last stray site (creator stage); `update_controller_status` formats only on hotplug/rebuild. One introduced-and-caught bug: the status line's first respawn guard used `Added<Text>` beside `&mut Text` — a B0001 init panic in every full-app target that `cargo test --lib` can't see; keyed on the marker instead, and the full suite is the loop's non-negotiable last step for exactly this class. Declines recorded: settings-rebuild-per-edit under the overlay (user-action frequency), `resolve_touch_owner` per-frame (O(1), staleness-proof beats a `run_if` web), `menu_tap` reader/applier split, a generic raw-touch→Button bridge (two sites now share `claim_pause_region_taps`; a third joins it), paint-Locals→derived-resource, per-slot P2 touch (standing), game-over confirm union, second-finger SWING re-tap (keyboard-mash parity — flow takes the first swing). Verified: 278 unit tests, native suite 31/31 targets green, autoplay suite 31/31 green, wasm check green. Conventions returned empty for the third consecutive cycle.

68. [x] Review cycle 11: eight verified fixes, touch-quit reachability, and a pipeline e2e that caught its first bug before landing. — Correctness: the lifted-chrome-finger release runs before `read_touch`'s early returns and in `paused_pause_region_taps` (my cycle-10 reorder had skipped it on ownerless frames — a recycled id could strand a visibly-touching finger); Paused re-decides pause-chrome liveness each frame with the same pre-frame reveal discipline as Playing (`seen`/ownership can flip mid-pause via `detect_touchscreen`/`resolve_touch_owner`, leaving the freshly painted button drawn-but-dead against the frozen latch); the chrome finger no longer counts toward the Zone Pad's cursor pinning (`any_contact_except` — a finger resting on the pause button after a refused dead-ball tap was center-pinning the cursor and locking keyboard steering out); ownership grants the same frame a touch lands (`resolve_touch_owner` consults live contact alongside the persistent bit — a pad-started tablet session was dropping its first gameplay tap entirely), with explicit `InputSystem` ordering edges on both the detector and resolver; an instantaneous pad tap keeps its aim (recorded at the claim — the continuous block never sees a down+up-in-one-frame finger, so swings graded at zone center instead of the tapped corner); the hint line is phase-aware (it advertised "LEFT PAD AIMS" while the regions had released to runner steering; live balls now hint the runner controls); `StartGame::start` refuses a second same-frame launch (keys + taps are two launchers on one seam — the later call silently overrode the mode and reassigned controllers); a Tap-scheme touchdown claims its finger spent (single windup tap fired two action edges). Reachability: the pause board has a tappable **[ QUIT TO MENU ]** row (Button + the same arm-then-confirm flow as Q/East; mouse gets it free) — a touch-only player could pause and resume but never leave a game. Structure: `any_contact` is the one encoding of "contact this frame including instantaneous taps" (three sites shared it by hand), `ZoneRegion`/`zone_region_at` single-encodes the button-beats-pad tie-break for the claim and handoff loops, `rules::AIM_DEADZONE` names the bare 0.5 shared by the throw selector, chaser steer, and drag guard, `rules::aim_to_world_x` converted its last stray (`aimed_base`), `Phase::in_delivery()` joins `pre_contact()` (tap window + rig stance window), the anticipatory-press nesting flattened, the pause rect is derived once per frame, `detect_touchscreen` stops running once the one-way bit sets, and `update_controller_status` lost both caches for a `Cow` (static strings for the common counts; nothing left to invalidate). Coverage: the zone-pad matrix cell's ownership assertion was vacuous (headless runs never see a touch, so the seen filter alone yielded `None` — `mark_seen` test seam added and the Director exclusion actually pinned now); new `e2e_touch_pipeline` drives raw `TouchInput` window events through the REAL schedule (ingest → detect → resolve → read → gather → `Intents`) and immediately caught the missing `InputSystem` edge on the resolver — the exact schedule-level class the minimal-harness unit tests can't see. Declines: settings keep-alive narrowing (live-verified in cycle 9), keyboard-fills-vanished-pad (recorded TODO-72 design), loud `unreachable!` rows (deliberate; all writers modulo-bounded), `pause_live`-derive-per-consumer (still needs a pre-frame latch — the surface shrink doesn't delete the discipline), `TouchScheme::prev` reverse-match (cycle 9 verified chained-`next` as house style), state-scoped `keep_ui_roots_alive` (3 entities, wasm-only), per-scheme adapter restructure and per-slot touch remodel (standing). Verified: 278 unit tests, native suite 32/32 targets green, autoplay suite 32/32 green, wasm check green. Conventions empty for the fourth consecutive cycle.

69. [x] Review cycle 12 (ten finders): the attract loop's game-over hang, the reveal discipline's scheme hole, and the audit gets eyes on touch. — Correctness: the autoplay attract loop had been hanging on the final card since the 1 s game-over grace shipped — `navigate_menus` held Enter, whose single `just_pressed` edge landed on the first GameOver frame, inside the grace; the driver now alternates press/release so fresh edges arrive. `pause_chrome_visible` lost its `scheme != Off` arm (the reveal discipline's hole: with a scheme persisted, the first-ever contact granted ownership and made the *unpainted* pause button live the same frame — the first touch of a session could pause the game from an invisible control); liveness is `owner && seen`, claims pass pre-frame `seen`. A confirmed quit tap can no longer be clobbered into a resume by a same-frame pause press (`board_controls` now carries `open_pause`'s decided-transition guard); a touchdown anywhere off the quit row cancels an armed quit (key-cancel parity — an accidental arm used to make the next quit tap, however later, a confirm); `press_quit` is the one arm/confirm encoding for Q/East/tap. The Zone Pad honors pre-aim: `adapt_swings` now applies an absolute cursor during the windup instead of hard-resetting it (the reticle aimed blind through the exact window the drawn pad invites, then teleported at delivery); a one-frame send blip no longer steals ("hold Down" is literal — `wind_up` requires two consecutive send frames, so a Tap scheme's low swing tap can't send a runner nobody called); second fingers consumed inside active Zone Pad regions are excluded from stick adoption ("the brush becomes no other input" now holds — a two-thumb pad grip was silently stretching the lead); the chrome finger keeps first-holder semantics (a second chrome tap no longer strips the first finger's protection) and its binding clears at `game_start()` (recycled-id strand across games); `paused_pause_region_taps` re-decides liveness each Paused frame with pre-frame claims (a touch seen mid-pause painted a live-looking button whose taps died against the frozen latch), is explicitly ordered against `tap_pause` AND the gamepad-hotplug writer — the latter caught by the ambiguity audit the moment `::meta::touch` joined its gameplay list. Menu/settings: `menu_tap` parks the settings cursor at its tap-open site (the one open the key-path park never sees — a CLOSE-then-reopen tap landed with the cursor on CLOSE); the launch-frame rebuild skip no longer suppresses a missing-tree build (latent blank-menu); `update_controller_status` moved after the rebuild in the chain (the fresh tree rendered one frame with a blank status line) and lost both caches for a `Cow` over static strings; the menu rebuild is gated `settings_closed` (frozen ticks make the catch-up fire exactly once on close — the deferred rebuild for free); `menu_tap`'s query gained the `With<Button>` filter its three siblings carry. Structure: `rules::aim_to_zone_y` (the vertical sibling — pad cursor and `PciState::center` map the zone span through one fn), `LastPainted` is a component on the overlay root (dies with the tree — the `Added` repair query and its whole staleness class deleted), `lifted_pause_finger` returns a verdict so callers only dirty the `ResMut` on a real release, `read_touch`/`paint_touch_overlay`/`tap_pause` skip until touch is relevant (never-seen desktop frames), `detect_touchscreen` stops once the one-way bit sets, the pause board root/card/dialog carry `KeepAliveUi` (the doc's claimed exemption was FALSE — `update_board` is change-gated, making the hidden z-30 root the exact 2026-08-27 stall shape), the hint container is marked like the settings card, `CONTROLS_TEXT` gained the touch line (the pause dialog listed only keyboard/pad to exactly the audience the quit row invites in), and the autoplay setup comment names the right predicate. CLAUDE.md records two exemption scopes the conventions finder flagged: raw window events in e2e bodies (double-buffered, survive to `InputSystem`) and shell chrome outside the `Intents` seam (the rule covers what plays baseball). Verified live on wasm: pause board fully rendered after a 25 s quiet idle (the KeepAliveUi fix), quit arm/confirm end-to-end, mid-pause first touch revealed the pause button without pressing it, trusted-input click cycled a menu row (Button path proven in-build; note for future verification — untrusted JS-dispatched events reach `Touches` but never bevy_ui picking). Suites: 278 unit, native 32/32 targets, autoplay 32/32, wasm check, all green. Declines re-affirmed: settings Row/Step enums, `pause_live` full derive, `apply_cycle`→`cycle_row` unify, board text-case formatter share, `keep_ui_roots_alive` state-scoping (3rd), per-scheme dispatch / per-slot touch / raw-touch Button bridge / effective-style resource (standing), instantaneous-tap `Interaction` drops (engine-level; retry-tolerable), PreUpdate audit expansion (explicit edges + the pipeline e2e cover it), `ScriptAction::Touch` device injection (the pipeline e2e covers the machine).

70. [x] Review cycle 13: five more translator/UI bugs, one self-caught mid-cycle regression, and strong convergence. — Correctness (line-by-line): an instantaneous flick — movement and lift in one frame batch, a routine browser hitch — was silently dropped (the release branch had no Flick arm; the sharpest flicks were exactly the lost ones); the hint line advertised "BALL IS LIVE" runner controls through the dead-ball Result pause (now phase-matched: live text for `InPlay`, quiet for `Result`); the between-pitch absolute-cursor write is clamped to the zone (`ScriptAction::Cursor` is unvalidated RON); the SWING button hit-tests the circle it draws, not its bounding square (~21% of the square was a visual miss that still swung); the bare-tap test bounds path length, not net displacement (an aborted out-and-back drag fired an unaimed pitch). Removed-behavior caught my own cycle-13 `park()` refactor dropping the `!open.0` gate — the cursor parked every OPEN frame too, making every settings row below the first keyboard-unreachable (taps masked it; no unit test runs `toggle_settings` and `edit_settings` together) — fixed within the same cycle; the two-frame send confirm now also honors a pre-pitch stretched lead (`|| lead.extended` — a one-frame wind-up on a hitching tab still sends a hold that predates it); `menu_tap` refuses to mutate `GameConfig` after a same-frame launch materialized the Ruleset from it. Altitude/reuse accepts: the mouse got the armed-quit cancel (cycle 12's touch bug, reproduced for the next device the Button serves "for free"); `game::transition_pending` is the one decided-transition guard (six sites, two hand spellings unified); `SettingsCursorRow::park` is the one parking encoding (the `set_text_if_neq` ResMut pattern, so the guard's read never dirties the paint's change tick); `Phase::pre_contact`'s doc now names the camera's exhaustive match as its one deliberate inline sibling (a guard arm would forfeit the compile error a new phase must trigger); the stale autoplay ownership comment names the right predicate. Convergence: conventions AND cross-file returned empty (cross-file's two candidates were already fixed mid-cycle by the time it reported); simplification and efficiency produced zero accepts — every candidate a re-litigation of a recorded verdict or self-ranked below the frame budget. Declines re-affirmed/noted: Row/Step enums, `pause_live` derive, `TouchScheme::prev` chained-next house style, the `Cow` status line (efficiency asked for it last cycle; simplification wanted it gone this cycle — it stays), park-on-open consolidation (the design any FUTURE open path should adopt; three comment-linked sites stand), swing-window naming (`== Phase::Pitch` is single-variant equality, not a drift-prone multi-arm window), send-channel remodel (the two-instant confirm hardens the documented "hold Down" for every device), `zone_pad_active` full-signature centralization, KeepAliveUi auto-enrollment bundle, zone-rect hoist (nanoseconds). Verified: 278 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.

71. [x] Review cycle 14: the chrome-finger model generalizes, the quit row gets its raw path, and the dismiss learns to aim. — Correctness (line-by-line + cross-file): the pause-chrome binding is now EVERY chrome finger (`pause_fingers: Vec<u64>`, released as each lifts), not just the first — an unbound second finger on the pause button was still center-pinning the Zone Pad cursor and, after the first lifted, adoptable as the stick (the exact ghost-input classes the single binding was built to stop, one finger later); the quit row gained the same raw just-pressed hit-test the pause button carries, against its own laid-out rect (`ComputedNode` + `GlobalTransform`) — without it an instantaneous confirm tap produced no `Interaction`, fell into the cancel branch, and DISARMED the quit it meant to confirm, and a second-finger quit tap while the pause grip rested resolved at the first finger's position and resumed the game instead; pointer dismissal of the game-over card must land ON the card (`GameOverCard` hit-test — a routine refocus click dismissed the score the player came back to read), with the hint text updated; the game-over grace is stamped and compared on the REAL clock (no future juice writer leaking `relative_speed` past `OnExit(Playing)` can stretch the lockout); `press_quit` carries `transition_pending` structurally (the one `NextState` writer that had it only by ordering); `(board_controls, auto_resume_on_refocus)` is chained (unordered, the pair raced on `NextState` and a same-frame keypress could be swallowed by the auto-resume — the TODO-80 claim-as-manual now provably precedes it); the `zone_cursor` doc says what the code does (sticky per PITCH, releasing with the regions — keyboard-PCI parity, not "per at-bat"). Structure (simplification/altitude/reuse/efficiency): `PrimaryTouch::tracked_from` is the one finger constructor (the struct grew twice; two literals differing only in `fresh` were a drift trap); `generic_action` is one spelling for the second-finger claim and bare-tap release halves; `KeyScheme::for_team` is the one team→keyboard mapping (four copies — the pad-fill arm could have handed a slot the other player's keys); `input::pointer_pressed` names the touch+mouse pair (cycle 13's cancel bug came from enumerating one half); `touchscreen_seen` is the named run condition (an inline closure duplicated it); `hint_line` truly owns shown-vs-hidden (the Paused blank moved inside; the dead-ball arm derives from `pre_contact()` so a future phase falls silent instead of falling through to a swing hint); the quit row spawns through the board's one `line_bundle`; `TeamIntent` device constructors use struct-update for channels they definitionally can't produce; `PITCH_AIM_X_M`/`SPRAY_AIM_FRAC` name the twin 0.6 aim-authority dials (coincidentally equal, deliberately distinct); `paused_pause_region_taps` gained the sibling `touch_relevant` gate and `wind_up`'s reset the guarded-write style; the stale `Added<TouchOverlay>` comment now names `LastPainted`. Declines: pad-over-touch overlay order (third raise — recorded policy, the 0.2 deadzone is the drift mitigation), final-windup-frame send press ("hold THROUGH the windup" — a last-instant press held through nothing), the wasm per-node stall re-raise (twice live-verified), the circle drawn/hit-tested coupling (no shared unit), scripted shell-flow coverage (the recorded chrome exemption's known cost), four prose homes for touch instructions (four audiences). Verified: 278 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green. Conventions empty for the third consecutive cycle.

72. [x] Review cycle 15: two angles empty, and the instantaneous-tap fallback reaches every surface. — Correctness (line-by-line + cross-file + altitude): the menu lines and settings rows — the two most-used tap surfaces, and the only ones still without it — gained the raw touchdown fallback (`touched_node`, per-entity bool-union with the `Interaction` edge so a held tap can't double-activate): an instantaneous tap on "1 One Player" or the CLOSE row was silently dropped, the class the diff documents in three other places; quit-row fingers are bound into the chrome registry (`bind_chrome_finger` — a still-held quit finger crossing a resume was adoptable as the stick, cycle 14's class on the newest chrome); the SWING-button holder (and pad finger) no longer force the Zone Pad center-pin (its hold produces no stick aim, so the pin's stray-finger rationale doesn't apply — a keyboard steerer resting an anticipatory thumb was locked to zone center); `tap_quit_row` carries an explicit `.after` the chrome release pass (both write `TouchGestures`; release-then-bind is the coherent order); the defense hint is phase-matched like the batting arms (it advertised pitching through the dead-ball Result; live balls now hint chase steering). Structure (simplification/reuse/efficiency): the split borrow deletes cycle 14's per-frame `pause_fingers` clone AND the `flick_spent` shadow/write-back sync trap; `read_touch` commits its output at ONE exit (a labeled block — a future early return can't leave last frame's intent latched); `release_lifted_chrome_fingers` is the one release body for both states (the discipline already regressed once by being per-site); `touched_node`/`node_screen_rect` single-encode the layout→logical-touch math for all four hit-test sites; `PciState::clamp_to_zone` is the one zone clamp for the in-pitch and between-pitch cursor paths; the override verdict computes inside `cycle_row_tracked` (a third edit path can't forget the preamble and silently edit the dormant value); the redundant `touchscreen_unseen` folded into `not(touchscreen_seen)`. Audit judgment recorded in the test itself: `::meta::subs` deliberately stays OUT of the ambiguity audit's gameplay list — the board is a web of order-insensitive-by-construction pairs (`NextState` writes are deferred; `update_board` is a presentation reader with accepted lag), and its one real conflict carries an explicit edge instead. Empty angles: conventions (4th consecutive) and removed-behavior (first — every deletion traced to a re-establishment site or recorded decline). Verified: 278 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.

73. [x] Review cycle 16: residue sweep, a lost-edit audit, and mid-pitch region promotion. — Three angles empty (conventions 5th consecutive, removed-behavior 2nd, cross-file — which ran the full test battery itself). Process catch, recorded so it can't recur silently: two earlier Python-scripted edits had silently no-matched (an indentation drift, and a batch aborting at an assert AFTER earlier replaces but BEFORE the write) — so the cycle-15 SWING-holder cursor-pin fix and most of the cycle-16 dedup batch existed only in TADA's record, not the code; caught by cycle 16's own line-by-line finder cross-checking the record against the tree. All redone with loud-failure edits and grep-verified: the cursor-pin closure excludes the SWING/pad holders (the live lockout), `chrome_free` is the named two-term core of the three gameplay-finger filters, the chrome binding has ONE body (free fn; the method and the pause claim both delegate), the doc splice un-spliced (`mark_seen` owns its test-seam paragraph), the dead `touchscreen_unseen` deleted, and the quit row's accept-test and finger-binding share one rect and one scan. New fix (line-by-line): when a Zone Pad region's OWNER lifts mid-pitch, a roleless thumb already resting in the region is promoted (`resting_roleless_in` — the claim loop only sees touchdowns and the handoff only runs at the at-bat's rising edge, so a second resting thumb was dead until re-pressed while the lifted owner's stale sticky aim kept emitting; the tracked stick is never promoted, preserving the wander rule), for the pad (cursor updates from the promoted thumb) and SWING (hold continuity feeds the anticipatory press) alike. Declines (altitude, all remodel-scale at convergence tail, each with its middle already shipped): TapTarget generic activation system (the pixel math and union policy are shared helpers; per-site residue is a few lines), TouchGestures split by lifetime (the three carve-out sites are stable and documented), per-frame finger-role classifier (`chrome_free` is the accepted middle), generalized chrome-claim consumed by shell (the bind now has one body; the pairing is ten lines of shared helpers). Verified: 278 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
74. [x] Review cycle 17: shared-helper consolidation, then five verified finder fixes — headlined by a multi-finger double-activation class. — Refactor batch (accepted from the morning sweep): `settings::open_settings` is the ONE park-then-open transition (menu tap-open and the S key share it), `input::button_pressed`/`touched_node_ids`/`pointer_on_node` give every tap surface one activation vocabulary (menu rows, settings rows, quit row, game-over card all converted; each surface gained the O(1) no-input early-out), `Controllers::default()` derives from `KeyScheme::for_team`, touch.rs interior dedup (`touch_team` private, resolver as a filter chain, `any_contact_matching`, hoisted `live_chrome`, `chrome_free` at every site, `resting_roleless_in` via `chrome_free(…, None)`), and `hint_line` restructured to ONE phase ladder. Finder fixes, each verified in-tree before accepting: (1) the Tap scheme's `dragging` guard is now WindUp-only — during the pitch the Tap arm silences the drag's stick entirely, so deferring to it there suppressed the tap's aim while protecting an aim that was never emitted (swings graded at zone-center instead of the tapped corner); (2) the touch pause button gets `GlobalZIndex(31)` — it stays a live resume target while Paused but drew under the board's tier-30 0.9-alpha dim, an invisible-but-live control (safe under the wasm z-index rule: painted with real colors on show); (3) `Off` folded into the phase-matched hint ladder — its generic stick steers runners/chaser during live play like every scheme's, but its early-return skipped the live-ball lines and Result-pause quiet; (4) THE CLASS: bevy_ui attributes a press edge to whichever pointer position it sampled, so with one finger resting on row A and a second tapping row B, the `Interaction::Pressed` edge lands on A — and cycle 15's raw-touchdown union turned that long-declined misattribution into double-activation (one tap firing two menu rows; a resume tap arming or even CONFIRMING a quit). Fixed by mouse-gating the `Interaction` half everywhere it pairs with a raw hit-test (`button_pressed`, the quit row, `tap_pause` — whose touch path already rides the translator's raw claims); touch activation is now solely the raw per-rect touchdown test, which cannot misattribute; (5) `open_pause` checks `transition_pending` BEFORE taking the pause-tap latch — the latch's whole point over a key edge is surviving to evaluation, so a tap racing a decided transition now defers a frame instead of vanishing (state-exit `clear_pause_tap` retires it if the transition leaves the setter states). Declines: removed-behavior's six (standing declines TADA 63/71, the grace window discarding-by-design — deferring would let an accidental mash dismiss the card at grace expiry, the live-verified TADA-65 keep-alive, and two future-path hazards each covered by two independent layers); the windup `lead.extended` bypass (false alarm: `pre_pitch` already set `steal_armed` at pitch commit whenever `lead.extended` held, so the arm is idempotent); Tap early-press feedback (keyboard parity — every style ignores pre-`Pitch` presses silently); the one-brush style flip (the persisted non-Off scheme is explicit user intent; reverting is one G-press). Verified: 278 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
75. [x] Review cycle 18: cycle 17's own fixes get dog-fooded, plus one determinism edge — the tail is nearly dry. — Conventions empty a 7th consecutive cycle (every CLAUDE.md invariant re-verified against the diff, including the three this work amended); simplification, altitude, and removed-behavior produced only standing declines or verified non-issues; line-by-line's full pass (which independently verified the mid-cycle `touch_relevant` gate and discharged the bevy_ui attribution model against vendored 0.15.3 sources) surfaced nothing high-confidence. Accepts, all small: (1) `resolve_touch_owner`'s per-frame registration gains `.and(touch_relevant)` — the exact sibling gate `read_touch` uses, a superset of every input that can set or clear ownership, so the desktop majority skips the resolver entirely; (2–4) reuse: cycle 17's mouse-gate policy had grown three bodies in one day — extracted as `input::mouse_pressed_on` (the ONE home of the multi-touch attribution rationale) consumed by `button_pressed`, `tap_pause`, and the quit row; `claim_pause_region_taps` was quietly a second containment scan violating `touched_node_ids`' "ONE body" doc promise — split a rect-space core `touched_rect_ids` and layered both on it; the menu/settings no-input early-outs now spell their device pair via `pointer_pressed` like the quit row already did; (5) `paint_touch_overlay` gains `.after(PauseInputSet)` — unordered against the Paused-state `TouchGestures` writers it read, a mid-pause seen/ownership flip painted chrome a frame later on some schedule builds than others (cosmetic, but a per-build tie-break in a repo that pins them). Declines of note: the two-frame windup send confirm and game-over grace re-litigated with sharper scenarios (hold-spans-one-frame at low fps; press-and-hold never re-edges) — both stand, the grace because every alternative (defer, level-accept) reintroduces the mash-through it exists to stop, worse now that pad-A doubles as the gameplay action button; Left/Right on the settings CLOSE row deliberately closes (it IS the row's activation — the screen has no Enter handler, so without it the row is keyboard-dead); same-frame two-finger tap aim is HashMap-order nondeterministic but the input itself is ambiguous (no principled winner, one swing either way; tests already frame-separate). Verified: 278 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
76. [x] Review cycle 19: three angles empty, four marginal accepts — the loop is at its tail. — Conventions empty an 8th consecutive cycle; efficiency and reuse both empty for the first time (efficiency explicitly endorsed the Cow status line a simplification finder wanted removed — the middle stands). Accepts, all small: (1) `game_over_restart`'s pointer closure drops its `pointer_pressed` pre-check — `pointer_on_node` already requires a just-pressed touch or click, so the pre-check was a second spelling of the same press condition that could drift; (2) the `touch_seen` doc moved from the `impl TouchGestures` block onto the method it describes; (3) a unit test now pins `duel_framing_wanted`'s deliberately-inline pre-contact arm to `Phase::pre_contact()` across all phases (with the conditional InPlay/Result arms forced false) — the hand-edit rule the predicate's doc relies on is enforced instead of remembered; (4) the Playing-side `(open_pause, auto_pause_on_focus_loss)` pair is chained like its Paused-side sibling, and in the player-favoring order: both write `SubsMenu.auto_paused` with opposite values and both bail on pending transitions, so on the frame an explicit pause raced an armed focus-loss pause, a per-build tie-break decided whether the next refocus silently resumed a game the player deliberately paused. Declines: the windup two-frame confirm, game-over grace, and CLOSE-row wrap re-reported and declined a third consecutive cycle (no new elements); the quit-row cancel-claims-manual finding is a false alarm (every path that arms a quit claims the pause manual first, so armed-while-auto is unreachable); same-frame tap-open + S-key double-open needs two hands within one frame, fails benignly, and its fix touches `toggle_settings`' park gate where a prior regression bit; f32 elapsed-seconds quantization needs ~24 days of continuous kiosk uptime; the altitude/simplification remodels (single-rect chrome, row table, SystemSet ladder, per-slot ownership, tap-scan skeleton, guarded-reset ceremony) are all standing declines. Verified: 279 unit tests (the new camera pin included), native 32/32 targets, autoplay 32/32, wasm check — all green.
77. [x] Review cycle 20: two invisible ordering gaps, one geometric overlap, one prompt — and a finder fix refuted at the type level. — Conventions empty a 9th consecutive cycle. Accepts: (1) `update_board`/`update_controls_dialog` were registered with NO ordering against any `SubsMenu` writer — the quit-confirm prompt painted on the arming frame or one frame later per schedule build; one `.after(PauseInputSet)` edge covers every writer transitively (the quit row is `.before` the set, the rest are in it); (2) the ambiguity audit's `::meta::touch` comment claimed the entry guarded "`tap_pause` vs `paused_pause_region_taps`" — but `is_gameplay` requires BOTH names to match and `tap_pause` lives in `::present::ui::touch`, which no fragment matched: the pair was silently scored as presentation. Listed `ui::touch::tap_pause` by its full name (both systems write `PauseTapped`, so dropping the registration's `.before` edge now fails the audit instead of shipping per-build), keeping the rest of `::present::ui` out on the same one-frame-lag rationale as `::meta::subs`; (3) on narrow viewports the quit row's laid-out rect can intersect `pause_button_rect`, so one tap both latched a resume and armed (or confirmed) a quit — the quit row now yields any finger the pause claim bound first (`TouchGestures::is_chrome_finger`, the read side of the bind), which is also the z-honest priority since cycle 17 drew the button above the board; (4) the armed-quit hint said "any other key cancels" on a surface that is now tappable and reachable by touch-only players — reworded device-complete ("Q/B or quit tap again confirms - anything else cancels"). Refuted mid-cycle: converting `paint_region`'s color writes to `set_if_neq` (a reuse finding citing the settings painter) does not compile and CANNOT work — `set_if_neq` lives on `Mut<T>`, and the caller's `&mut` reborrow already marks the components changed at the call site; the settings painter differs precisely because it calls `set_if_neq` on the un-dereferenced `Mut`. Reverted with a comment recording why, so the next finder doesn't re-flag it. Declines of note: gating `tap_quit_row` on `touch_relevant` (an efficiency suggestion) would make the quit row unclickable by MOUSE on every touchless desktop — `touch_relevant` is false there and the row serves both devices; the pause button consuming its own mid-play taps is drawn-equals-claimed working as designed (the refused-press banner is the feedback); the windup send confirm and one-brush style flip hit their sixth/seventh and fourth re-reports; persist-per-change keeps durability over debounce; two deliberate same-frame finger taps cycling two menu options mirrors the settings rows' accepted multi-touch semantics. Verified: 279 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
78. [x] Review cycle 21: the resting-thumb center-lock — the cycle-15 bug one finger later — plus two pins; four angles empty. — Conventions (10th consecutive), altitude, efficiency, and reuse all came back empty, altitude explicitly checking every residual candidate against this ledger before declining to re-litigate. Accepts: (1) THE FIND (line-by-line): the Zone Pad cursor-pin's `gameplay` filter excluded region OWNERS by id but not roleless fingers RESTING in a region by position — a second thumb resting on the already-owned SWING button (which the claim loop had consumed: "the brush becomes no other input", emitting no stick aim) still counted as a stray contact and center-pinned the cursor, locking a keyboard PCI steerer to zone center; the same gap pinned the cursor on the very frame owner-lift PROMOTED that resting finger. One position carve-out mirroring the adoption filter directly below (`!(zone_pad_at_bat && zone_region_at(..))`) closes both, now pinned by `translator_zone_pad_region_resting_fingers_do_not_center_pin` (280th unit test); (2) the anticipatory-press block hand-spelled `batting && scheme == ZonePad` inside its Pitch branch — exactly `zone_pad_active` at that phase, the predicate whose doc claims to be the "one home for the when"; adopted; (3) the zone-pad matrix cell persists `touch_scheme: ZonePad` into the per-PROCESS settings store every later cell in the binary loads — masked only by fresh apps' `seen` being false, one `mark_seen()` from an order-dependent cross-test failure; `start_matrix_game` now pins the scheme per cell exactly as it already pins `batting_style`. Declines: the windup send confirm and one-brush ownership reached their eighth/ninth and fifth re-reports; the instantaneous-tap one-frame pin is the lockout's deliberate conservatism (the carve-out for it would special-case same-frame lift to protect a window needing a same-frame keyboard swing coincidence); the two-finger Cycle+Start iteration-order variance falls under the simultaneous-deliberate-taps decline; the three "roleless" filter spellings are semantically DIFFERENT predicates (id-exclusion vs region-geometry vs +primary), so the proposed unifying helper is the declined classifier remodel in smaller clothes; drop-vs-defer on a coalesced settings-close+tap frame; the `.or_else` cursor-pin style rewrite (no defect, adjacent lines). Verified: 280 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
79. [x] Review cycle 22: the loop audits its own previous fix — the wandered-stick snap restored — plus four smaller accepts. — Conventions (11th consecutive) and reuse empty. Accepts: (1) THE CATCH (removed-behavior, on cycle 21's own carve-out): excluding region-resting fingers from the cursor-pin by position also excluded the TRACKED STICK when it wandered into a region — and unlike a consumed roleless brush, the wander rule keeps the stick emitting aim there, so its drag velocity-integrated the swing cursor, the exact drift the always-snap pin is documented to stop. The carve-out now exempts the primary (`gestures.primary.is_some_and(|p| p.id == id)`), and BOTH directions are pinned: `translator_zone_pad_region_resting_fingers_do_not_center_pin` and the new `translator_zone_pad_wandered_stick_still_pins_the_cursor`; (2) `cycle_row`'s `batting_style[row]` indexed the slot array by ROW const — correct only while ROW_P1_STYLE/ROW_P2_STYLE happen to be 0/1; a row inserted above them became an out-of-bounds panic or wrong-player edit with no compile error and no test failure — mapped row→slot explicitly; (3) the flick commit (trigger test + swing emit + spent burn) had two verbatim bodies in the held-finger and just-released arms — extracted `try_flick`, the ONE flick commit; (4) `flick_fire` divided by the raw frame dt, so a sharp flick batched into one hitch-stretched frame measured UNDER the trigger — the exact case the release arm exists to catch, and the same flake the unit tests' own 450px-sweep comment documents dodging — the sample dt now clamps at `FLICK_MAX_SAMPLE_SECS` (0.1 s), pinned by a hitch-frame assertion; (5) the `Off` hint's "PICK A SWING SCHEME (G)" renders ONLY during gameplay, where G and the settings screen are both MainMenu-gated — an instruction the player can never follow where shown; trimmed (the menu's "G Touch swing" row advertises schemes where actionable). Declines: the windup send confirm reached re-reports nine through eleven (one tracer re-verifying the CPU AI holds Down across frames, so it still arms); `TouchScheme::prev`'s chained-next matches the file's convention; the per-frame park removal targets the exact spot cycle 13's regression bit; the bind-chrome-finger one-body inversion would flip-flop cycle 16's accepted structure; the n≥2-pads menu format! stays the accepted Cow middle. Verified: 281 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
80. [x] Review cycle 23: five empty angles, one accept — the loop refines its own cycle-22 fix. — Conventions (12th consecutive), efficiency, altitude, and the cross-file tracer all empty; two finders killed mid-run by API timeouts were resumed from their transcripts and completed. The accept: cycle 22's flick dt clamp had reduced the hitch-frame trigger to a pure ~0.11-fraction displacement test — low enough that a slow resting-thumb DRIFT across a 0.5 s hitch (true speed far under the trigger) fired an unintended swing and burned the pitch's one flick. `flick_fire` now has two arms: true speed on normal frames (dt ≤ `FLICK_MAX_SAMPLE_SECS`, unchanged from pre-clamp behavior), and a flick-scale displacement floor (`FLICK_HITCH_MIN_FRAC` = 0.2) on hitch frames where speed is unmeasurable — pinned in BOTH directions (sharp batched flick fires; slow drift does not). Declines and refutations: the reuse finder's claim that sibling enums use explicit reverse-match `prev()`s is FALSE against the tree (all four use chained-next — the cycle-19 decline stands on its actual footing); OS-synthesized compat mouse clicks defeating the mouse gate is plausible-unverified (web — the primary touch target — suppresses compat mouse events via winit's pointer-event preventDefault; native Windows filtering is winit's layer; revisit only with a real-device repro); tap-the-card setting `seen` is the ownership design's sixth re-report (a player following the card's tap instruction IS a touch user — the persisted scheme applying is the feature); plus the standing while-closed park, tick-guard ceremony, tap scaffold, Cow line, CLOSE-row wrap, grace window, and e2e-harness-hoist declines. Verified: 281 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
81. [x] Review cycle 24: the loop catches two of its own regressions — eleven accepts, the biggest yield since the multi-finger class. — Run twice interrupted (API timeouts, then the Fable usage limit killed five finders mid-flight); all were resumed from their transcripts and completed, and the two resumed last found the most. Conventions empty a 13th consecutive cycle. REGRESSIONS IN THE LOOP'S OWN FIXES: (1) cycle 23's two-arm flick trigger is STRICTER than the speed test it stands in for in the band dt ∈ (0.1 s, 0.182 s) — at 0.15 s it demanded 0.2 of the smaller dimension where speed asked 0.165 — silently dropping exactly the mild-hitch flicks the hitch handling exists to save; replaced by ONE continuous expression, `bar = (FLICK_TRIGGER_HPS * dt).min(FLICK_HITCH_MIN_FRAC)`, which can only ever loosen, and pinned by a sweep asserting anything past the plain speed trigger fires at every dt; (2) cycle 20's quit-row chrome skip had a tail — a chrome-claimed tap skipped the row and then FELL INTO the cancel branch, so where the rects overlap the confirming tap threw the arm away and the player pressed QUIT twice and kept playing; the cancel now ignores presses the chrome already owns. Other accepts: `zone_pad_was` was updated BELOW `read_touch`'s early returns, so one ownerless or window-less frame left it latched true and the next at-bat's `zone_pad_started` never fired — thumbs resting on the pad and SWING button dead for that whole at-bat; hoisted with the other per-frame hygiene. The touch overlay and HUD roots share the default tier and were spawned as an unordered tuple, making SWING-button-over-scoreboard a per-schedule-build tie-break on a portrait phone; chained (chrome is interactive, so it spawns last and draws on top). Unpainted chrome parked off-screen: with `Auto` insets an absolute node falls back to its static position, so on a machine that never sees a touch (the painter is `touchscreen_seen`-gated) the pause `Button` sat at the overlay's origin at tier 31 — a tiny invisible click-blocker above every tier. `menu_tap`'s `Cycle` arm now stops like its siblings (REVERSING the TADA 77 decline: the file's own `OpenSettings` arm had already established one-activation-per-frame, so this is applying the rule, not re-litigating ambiguity — falling through applied several actions in spawn order, and cycling Field also rewrites innings). The between-pitch PCI cursor apply is style-gated like its in-pitch twin (`ScriptAction::Cursor` is style-agnostic, so a script pairing it with Classic/meter parked a position no adapter owns, live if the slot later resolved to PCI). Plus: `pause_live` decided once through the shared predicate above the ownership return (the ownerless `false` was that predicate hand-spelled); the settings cursor write guarded so a repeat tap on the current row doesn't dirty the paint tick; `try_flick` as the one flick commit; `region`/`row_part` spawn bundles (the paint side was already one body, and the spawn colors are where the wasm alpha invariant lives); the cursor-pin scan inlined so `||` short-circuits; and the workspace's only float-fallback warning fixed (a future hard error). Refuted outright: a claimed steal-arming path through `pre_pitch`'s single-frame `lead.extended` needs the Tap arm to fire during PrePitch, but `in_delivery()` is `WindUp | Pitch` — it cannot. Declines: the windup send confirm (12th and 13th re-reports), one-brush ownership (7th), the `hidden_root` bundle (unsound — `hidden_tint` also dresses actively repainted elements, so coupling it to `KeepAliveUi` would be wrong at most call sites), `transition_pending` as a type-level seam (factually right that four writers skip it, but each is the only writer live in its state or is debug tooling — no bug to fix), splitting `paint_touch_overlay` for parallelism (it would contend only with UI painters that already contend with each other and all early-return), and guarding the `TouchGestures` writes (wouldn't restore the tick anyway — `last_pos` dirties it on every held-finger frame). Verified: 281 unit tests, native 32/32 targets, autoplay 32/32, wasm check — all green.
82. [x] Review cycle 25: the settings rows adopt the one-activation rule, and the lint surface goes clean. — A single-pass review this cycle (the fan-out's model budget had been spent), three accepts, all verified independently afterwards: (1) `tap_settings_row` now returns after activating a row — the same fall-through cycle 24 had just fixed in `menu_tap`'s `Cycle` arm, so a multi-touch frame applied several row edits in spawn order; the unconditional return also subsumes the narrower stop-on-close return that was there before; (2) `#[allow(clippy::too_many_arguments)]` on `menu_tap`, `game_over_restart`, and `tap_settings_row` — the three wide systems this work added, where every other wide system in the repo already carries the allow; (3) two `field_reassign_with_default` sites in new tests switched to struct-update syntax. The lint surface is now clean: `cargo clippy --all-targets` emits nothing from this workspace (the one remaining line is the third-party `block` crate's future-incompat notice). Two behaviours the reviewer flagged as intended-not-defects match standing declines: the final-wind-up-frame send drop (the two-frame confirm's documented one-frame trade-off) and the touchscreen-laptop style override after one incidental contact. Verified: 281 unit tests, native 32/32 targets, autoplay 32/32, wasm check, clippy — all green.
83. [x] Review cycle 26: CLEAN — no findings, no changes. The loop ends here. — A full pass over the whole working tree (33 modified files, ~1,950 insertions, plus the four new files) produced nothing to fix. The reviewer re-derived the load-bearing invariants from scratch rather than trusting this ledger: every `aim_to_world_x`/`AIM_DEADZONE`/`aim_to_zone_y` substitution is algebraically identical at all six call sites (no half-flip); all seven `Phase::pre_contact()`/`in_delivery()` replacements expand to exactly the phase sets they replaced, with the camera's deliberate inline sibling pinned by its test; the `gather_intents` overlay fold reproduces pad-over-keyboard exactly, its one behavior change (a vanished pad falling back to keyboard) being the documented TODO-72 decision; the ROW consts, labels, paint arms and cycle arms agree with both `unreachable!` arms genuinely unreachable for all nine spawned rows; the chrome-finger lifecycle covers every state that reads touches; `pause_live`'s pre-frame `seen` keeps a revealing touch from pressing the button it reveals; the three chrome rects are pairwise disjoint (hand-checked at eight extreme aspect ratios beyond the test's four); `flick_fire`'s capped bar can only loosen as dt grows; and no new panic path exists outside tests. It also confirmed feature parity the hard way — `--features autoplay` running `balance_sim` and `e2e_matrix` green, which is what the `AutoplayPlugin` move to `main.rs` was for — and answered the one question it raised itself (the pad/SWING/pause nodes look like `KeepAliveUi` candidates but aren't: `hint_line` changes on every phase transition, so the appearance key forces a repaint several times per pitch and those nodes are never quiet). Verified: 281 unit tests, native 32/32 targets, autoplay 32/32, wasm check, clippy — all green.

## Batch 7 — Clean Code refactor pass (2026-09-07)

Driven by a Clean Code / Agile review of the whole tree. Default clippy was already
clean; every finding came from the pedantic tier (`too_many_lines`,
`cognitive_complexity`) plus a duplication and data-clump sweep.

Intended as behaviour-preserving, and *nearly* was: the follow-up review caught one
real regression this pass introduced (90), which the 344-test suite could not see
because it changed no value — only change-detection ticks. Item 90 fixes it and adds
the probe that now guards it.

84. [x] `read_touch` decomposed into four named passes. — 271 code lines / cognitive
    complexity 47 (the repo's worst) down to off the lint list entirely. The four
    `── section ──` banners inside it were already the seams; the missing concept was
    the ten per-frame locals threaded implicitly through them, now a `Frame` struct
    whose fields *are* each pass's declared inputs. `claim_new_fingers`,
    `steer_zone_pad`, `drive_primary`, `adopt_orphan_stick` — in the order a finger
    moves through them. `Frame::chrome_claims` / `Frame::in_zone_region` replace three
    hand-spelled copies of the same predicate. The `'compute:` single-exit block and
    every ghost-input guarantee are preserved verbatim; all 21 translator tests green.
85. [x] The overlay screens stopped hand-rolling their own chrome. — `ui::overlay_root`
    + `ui::overlay_card` + `ui::OverlayPaint` replace three near-identical full-screen
    scaffolds (menu, settings, pause board). This was a live inconsistency, not just
    duplication: the menu painted `panel_bg.with_alpha(0.97)` raw while the other two
    used `hidden_tint`, so the wasm "alpha-0 never renders again" rule was re-derived
    per site. `OverlayPaint::{Opaque,Hidden}` now names *why* a screen gets which paint.
86. [x] Overlay z-tiers became `ui::z` constants. — Four scattered `GlobalZIndex(10/20/
    30/31)` literals, each re-explaining the ladder in prose. Extracting them surfaced a
    stale doc claim: three comments said "banners (40)", but the banner pill has **no**
    `GlobalZIndex` on purpose (a cycle-2 attempt stopped wasm extraction and was
    reverted). `ui::z` now records that absence as a rule instead of the docs claiming a
    tier that must not exist.
87. [x] `flow` grew an umpire; `pitch_live` lost its god-function shape. — `(score,
    bases, rules, banner)` travelled together through all five rule-application helpers
    across `result.rs`/`live.rs` — a textbook data clump, and the reason `pitch_live`
    (142 lines) resisted extraction: every candidate inherited a 12-argument signature.
    New `flow/umpire.rs` borrows the quartet once, so each call takes only what
    distinguishes it. `pitch_live` then split cleanly into `judge_take`, `judge_whiff`,
    and `settle_batted_ball` (4-5 args each, nesting 5 levels deep down to 3) and is off
    the lint list. `announce` is private, so no banner can be sent without the scoreboard
    change that earned it.
88. [x] `touch.rs` and `core/coach.rs` became directories. — Per the repo's own
    same-named-subdirectory convention, and only *after* the function-level work (89):
    splitting first would have relocated complexity rather than removing it. `touch/`
    = the surface (resource, rects, ownership, plugin) + `translate.rs` (the per-frame
    machine, 1829 → 675 + 1269 incl. tests). `coach/` = types and the `observe`
    dispatcher + `checks.rs` (the nine uniform checks, 1341 → 879 + 482).
89. [x] `Play`'s 21 fields grouped into three lifetimes. — `pitch` (the ball in flight
    and how it was received), `duel` (the pre-pitch leadoff/pickoff), `live` (contact
    through the announced call). Reads better at every call site (`play.duel.armed`,
    `play.live.home_run`) and a new field now lands in exactly one cluster. All 83
    access sites were repointed from compiler-reported locations rather than by regex;
    `Play::default()` remains the single reset.
90. [x] Fixed a change-detection regression introduced by 87, and grew a probe for the
    class. — Hoisting `Umpire::new(&mut score, &mut bases, …)` above the branches in
    `pitch_live` looked like a harmless CSE, but `&mut` on a `ResMut` goes through
    `DerefMut`, which calls `set_changed()`. The borrow therefore marked `ScoreBoard`
    and `Bases` dirty on **every frame of the pitch flight** (measured: 400/400),
    defeating the `is_changed()` guards in `hud::update_{score_text,inning_text,
    count_dots,base_ring}` (the TODO 78 relayout fix), `player::rig`'s colour/identity
    painters, `jersey::dress_jerseys` (a `String` clone per jersey quad per frame via
    the `Changed<PlayerIdentity>` cascade), and `runner::sync_runners` — worst on wasm.
    The umpire is now built inside each arm that actually makes a call, as `live.rs`
    already did. New `tests/e2e_change_detection.rs` samples the ticks on in-flight
    frames and fails at 100% / passes at 0%; verified red-then-green against the exact
    regression. Nothing in the correctness suite could have caught this: `set_changed()`
    alters no value, so every play resolved identically either way. The probe was then
    widened from the two resources involved to **all seven** that gate a repaint
    (`ScoreBoard`, `Bases`, `BattingOrder`, `Rosters`, `Theme`, `Settings`,
    `Controllers`) — it guards the class, not the instance. All seven measure 0.0%
    today, so this was the only occurrence in the tree.
91. [x] Browser-verified the overlay extraction (85, 86) on the real wasm target. — The
    changes in 85/86 sat squarely inside the repo's most dangerous documented failure
    mode (alpha-0 at first extract never renders again), and `cargo check --target
    wasm32` only proves it compiles. Drove the debug wasm build in Chrome: the menu
    (tier 10, `Opaque`), the settings screen (tier 20, `Hidden` + `KeepAliveUi`, drawing
    correctly *over* the menu), the pause board (tier 30, including the hand-mutated
    column `Node` that stacks the controls dialog under the card with its gap), the
    full gameplay HUD, and the STRIKE banner pill rendering at the *default* tier —
    the one `ui::z` explicitly documents as having no `GlobalZIndex`. Count dots
    advanced on the judged pitch, which also confirms the 90 fix did not over-correct
    and starve the legitimate `score.is_changed()` repaint. No console panics.
92. [x] `sim::coach::observe` split into a tracker and a snapshot builder (TODO 99). —
    151 lines with the same tell `read_touch` had: section banners marking distinct
    jobs. It already had the right bundles (`WorldFacts`/`PlayReports`/`WorldRigs`) but
    destructured them into thirteen locals at the top, so nothing downstream declared
    what it used. Now `track_frame_facts` (per-frame events and phase edges the 30 Hz
    sampler would miss) and `build_snapshot` (read-only assembly of what the pure Coach
    grades) each take the bundles by reference — four parameters apiece — and `observe`
    is just track → sample-gate → build → dispatch. Off the lint list. The split pushed
    `observe` to 8 arguments; rather than restore the `#[allow(too_many_arguments)]` it
    used to carry, the three output channels became a `CoachOutput` `SystemParam` — the
    same answer the file already gives on the input side, and now no suppression at all.
93. [x] `subs::update_board`'s wording extracted as a pure `board_line` (TODO 99). — 112
    lines, most of it one `match` mapping a row kind to its text and colour. That match
    is now a pure function of values the caller already holds, returning `Option` so the
    "past the end of a short lineup" case and the "board is hidden" case share **one**
    blanking path instead of two `continue`s and a separate branch. The painter also now
    writes through `ui::set_text_if_neq` like every other painter in the codebase,
    rather than hand-rolling an unconditional `**text =` (glyph re-shaping is the
    expensive half of a `Text` write). Off the lint list.
94. [x] The effect palette became theme-owned (TODO 77). — A theme swap used to repaint
    only half the show: the landing ring and contact sparks re-derived themselves from
    `ui.accent` and `ball.trail` at their own spawn sites, while the infield dust and the
    five firework shells were hardcoded in `present/fx/particles.rs` — so a night game
    kicked up warm daylight puffs and burst warm daylight shells. All four now come from
    one `FxTheme` on `Theme`, and `present/fx/` contains no colour literals at all.
    Midnight Neon finally gets its own: cool blue-grey dust and neon cyan/magenta/violet
    shells. Daylight Classic's values are byte-identical to before, so its look is
    unchanged. Only *hue* moved — per-effect opacity stays at the spawn site, where it
    says how that effect reads (the HR halo is fainter than the sparks it surrounds)
    rather than anything about the theme. `settings.trail_color` deliberately stayed out:
    it is the player's choice, not the theme's. New unit test asserts every fx channel
    differs between themes and that no show repeats a shell colour, so re-hardcoding any
    one of them fails.

